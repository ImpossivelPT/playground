name: docsify-action
run-name: ${{ github.actor }} is generating docsify pages
on: [push]
jobs:
  bump:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@main
    - uses: tool3/docsify-action@main
      with:
        dir: docs-team
        branch: main
        github_token: ${{ secrets.GP_TOKEN }}
